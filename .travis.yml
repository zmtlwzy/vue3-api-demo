language: node_js
node_js:
  - lts/*
install:
  - pnpm install
script:
  - pnpm build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main